<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wolfhackers.counsellink">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CounselLink"
        tools:replace="android:label"
        android:supportsRtl="true"
        android:name="com.orm.SugarApp"
        android:theme="@style/AppTheme">
        <meta-data android:name="DATABASE" android:value="counsel_link.db" />
        <meta-data android:name="VERSION" android:value="4" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.wolfhackers" />
        <activity android:name=".MainActivity" />
        <activity
            android:name="com.wolfhackers.counsellink.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.wolfhackers.counsellink.MattersHome"
            android:label="@string/title_activity_matters_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.wolfhackers.counsellink.SerachDemo">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name="com.wolfhackers.counsellink.AddMatter"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/title_activity_add_matter"
            android:windowSoftInputMode="adjustPan"></activity>
        <activity
            android:name="com.wolfhackers.counsellink.ViewMatter"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"></activity>
        <activity android:name="com.wolfhackers.counsellink.AddNote"></activity>
        <activity
            android:name=".CalendarActivity"
            android:label="@string/title_activity_basic" >
        </activity>
        <activity
            android:name=".EventAddActivity"
            android:label="@string/title_activity_event_add" >
        </activity>

        <activity android:name=".Matters"
            android:label="Matters"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>